name: construct_component_provider_py
description: A program that constructs remote component resources with first class provider.
runtime: python
config:
  codeinfra:disable-default-providers: ["*"]
