[tool.poetry]
name = "codeinfra-python-poetry"
description = ""
package-mode = false
version = "0.1.0"

[tool.poetry.dependencies]
python = "^3.8"
codeinfra = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
