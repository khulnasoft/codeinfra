name: packageadd_uv
description: A minimal Google Cloud Python Codeinfra program
runtime:
  name: python
  options:
    toolchain: uv
config:
  codeinfra:tags:
    value:
      codeinfra:template: gcp-python
